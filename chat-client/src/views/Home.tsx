import { Grid } from '@material-ui/core';
import * as React from 'react';

const Home = props => (
  <Grid container>
    <Grid item xs={12}>
      Home view works!
    </Grid>
  </Grid>
);

export default Home;
